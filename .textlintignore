# APM dependencies
apm_modules/**

# apm が配置する依存 skill
.claude/skills/empirical-prompt-tuning/**

# テストカバレッジ
coverage/**

# テスト fixture (lint 対象外。内容変更がテストに影響するため)
e2e/scripts/samples/**

# 隔離 worktree (他タスクの checkout。lint 対象外)
.claude/worktrees/**

# textlint rule preset のキャッシュ (proofdict 等)
**/.cache/**
