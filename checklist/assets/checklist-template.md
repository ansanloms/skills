<!--
検証チェックリストの体裁の正本。checklist skill を参照する skill が、自分の SKILL.md に検証節を書くときにこの形へ合わせる。
骨格のプレースホルダ (<...>) を実内容に置き換えて使う。例と規則が食い違えば、下の記入例を正とする。
この案内コメントは成果物 (呼び出し側 skill の本文) には持ち込まない。
-->

# チェックリストの体裁 (骨格)

```markdown
## 検証

<検査対象と実行タイミングを 1 文で書く。>

判定・報告は checklist skill に従う。checklist skill が導入されていない環境では、本節の項目を ○ / × / 対象外で自己点検し、× の該当箇所と根拠、対象外の理由とともに報告へ含める。

### <観点名>

1. `[critical]` <望ましい状態を述べる 1 文>
2. <望ましい状態を述べる 1 文>
3. <望ましい状態を述べる 1 文>。判定: `<コマンド>` の<結果の写像 (終了コード・件数の解釈)>。見ないもの: <チェッカの限界>。

### <観点名 (観点が複数ある場合)>

1. `[critical]` <望ましい状態を述べる 1 文>
2. <望ましい状態を述べる 1 文>
```

書き方の規則は次のとおり (詳細は checklist skill の「項目の書き方」)。

- 番号は観点ごとに 1 から振る。報告では「<観点名> <番号>」で参照する。
- `[critical]` は成果物として成立しない欠陥を検出する項目に付け、リスト全体で最低 1 件置く。
- 機械判定の項目は、実行コマンド・結果の写像・チェッカの限界を項目内に書く (骨格の 3 項目め)。
- 致命的な項目を観点の先頭側に置く。

# 記入例

架空のリリースノート生成 skill の検証節として記入した例。

```markdown
## 検証

生成したリリースノート (docs/releases/<version>.md) を、書き終えた直後に検査する。

判定・報告は checklist skill に従う。checklist skill が導入されていない環境では、本節の項目を ○ / × / 対象外で自己点検し、× の該当箇所と根拠、対象外の理由とともに報告へ含める。

### 形式

1. `[critical]` ファイル名がリリースの version (vX.Y.Z) と一致している。
2. `[critical]` 見出し構成が「変更点 / 破壊的変更 / 既知の問題」の順で欠落なく揃っている。
3. markdown の lint が指摘を出さない。判定: `deno task lint docs/releases/<version>.md` の終了コード 0 で ○、1 で ×、2 以上は実行不可として対象外。見ないもの: リンク先の実在 (形式 4 が読解で判定する)。
4. 本文中のリンク先 (issue・PR の URL) がすべて実在するリリースの成果物を指している。

### 内容

1. `[critical]` 記載された変更点がすべて対象リリースのコミット履歴に対応している。履歴に無い変更を書いていない。
2. 破壊的変更に移行手順が添えられている。
3. 変更点の記述が利用者視点 (何ができるようになったか) で書かれ、内部実装の用語だけで終わっていない。
```
